# from django.db import models

# class ImageUpload(models.Model):
#     image = models.ImageField(upload_to='uploads/')  # Image upload field
#     prediction = models.CharField(max_length=255, blank=True)  # Field to store prediction

#     def __str__(self):
#         return self.image.name
